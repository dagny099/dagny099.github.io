# Archived `_data` files

These were moved out of `_data/` on 2026-06-22 because nothing in the site
referenced them. They live here (a `_`-prefixed dir Jekyll ignores) so they
stay in version control but are **not** loaded as `site.data` and are **not**
published.

| File | Why archived | Revive value |
|------|--------------|--------------|
| `resume.yml` | Superseded by `_data/barbara_resume_golden.json`, which drives `/experience/`. Also stale (old `barbs@balex.com` email, outdated summary). | Low — historical only. |
| `ia.yml` | An early hand-built information-architecture tree. Superseded by `_data/navigation.yml` and out of date (lists only 2 projects). | Low. |
| `tags.yml` | Per-tag color + description metadata that was never wired into any tag page or template. | **Medium** — could be revived to style tag chips/pages by tag. |
